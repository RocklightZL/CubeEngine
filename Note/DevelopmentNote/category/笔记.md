此文档包含所有标记为 `笔记` 的内容，按日期排序：

---

# 8.8

## ImGui中的ID
ImGui中不同窗口（ImGui窗口）中的ID是可以重复的。

---
# 8.13

ImGui中窗口停靠后标题栏实际上就是标签页，样式也由标签页样式决定。

---
# 8.13

去除标签页被选中时上方出现的短线：
去除ImGuiTabBarFlags_DrawSelectedOverline标志，或者将 style.TabBarOverlineSize设为零。

---